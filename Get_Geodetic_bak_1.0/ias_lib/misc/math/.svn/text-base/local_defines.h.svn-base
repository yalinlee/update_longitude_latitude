#ifndef LOCAL_DEFINES_H
#define LOCAL_DEFINES_H

#define MAX_DIM  6      /* dimension for matrices used in fitting correlation 
                           offsets */
#define MINSIGMA 0.01   /* minimum sigma when calculating image subimage stats*/
#define NPEAKS 32       /* number of peak correlation values to track */
#define SWAP(a,b) tempr=(a);(a)=(b);(b)=tempr

#endif
