/*---------------------------------------------------------------------------
NAME:    ias_pm_destroy_iterator

PURPOSE: Free the resources allocated to an iterator.  After calling this
         for an iterator, the iterator can no longer be used.

RETURNS: nothing

-----------------------------------------------------------------------------*/
#include <stdlib.h>
#include "pm_local.h"


void ias_pm_destroy_iterator    
(
    IAS_PIXEL_MASK_ITERATOR *iterator /* I: Pointer to iterator structure */
)
{
    free( iterator );
}  /* END ias_pm_destroy_iterator */
